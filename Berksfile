source 'https://supermarket.chef.io'
metadata

# use creativemarket fork which uses fewer dependencies
cookbook 'monit-ng', github: 'creativemarket/monit-ng'

# the Supermarket versions are lagging behind and fail some tests from https://github.com/dev-sec/linux-baseline
cookbook 'os-hardening', github: 'dev-sec/chef-os-hardening'
cookbook 'ssh-hardening', github: 'dev-sec/chef-ssh-hardening'
